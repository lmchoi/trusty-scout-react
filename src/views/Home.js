import React from "react";

const Home = () => (
    <div>
      <header>
        <a href="https://localhost:8080/auth/yahoo">Sign On with Yahoo</a>
      </header>
    </div>
);

export default Home;
